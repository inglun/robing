1 Skriv ut kolmnhuvud

2 Fixa till PrintBoard så att
if (map.ContainsKey(coord))
{
    val = map[coord];
    if (val == "mål")
    {
        val = "#";
    }
}
blir betydligt enklare. TIPS: komplettera if-villkoret så att man inte går
in i if-blocket över huvud taget om det står "mål" på koordinaten coord

3 Skriv en metod, GetCoordinateToShootAtFromUser som läser användarens input och returnerar en Coordinate,
public Coordinate GetCoordinateToShootAtFromUser() {
    // läs vad användaren skriver och returnera det
}
loopen (do-while) i Run ska alltså göra
... {
    PrintBoard();
    Coordinate coord = GetCoordinateToShootAtFromUser();
    ShootAt(coord);
} ...
